import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  outputFileTracingRoot: '/Users/basti/Documents/DAN/Web',
  images: {
    formats: ['image/avif', 'image/webp'],
  },
}

export default nextConfig
